name: Build and deploy ASP.Net Core app to Azure Web App - revart-api-79

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Restore
        run: dotnet restore backend/RevArt.Api/RevArt.Api.csproj

      - name: Build
        run: dotnet build backend/RevArt.Api/RevArt.Api.csproj --configuration Release --no-restore

      - name: Publish
        run: dotnet publish backend/RevArt.Api/RevArt.Api.csproj --configuration Release --no-build --output ./publish

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: revart-api
          path: ./publish

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: revart-api
          path: ./publish

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_7DA453A9BC5B43D7A2A8722E2814FFBD }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_2340768434904130A271D8DFC1A9090B }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_1D8695A23BA340D7BC09F969D9F8A672 }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'revart-api-79'
          slot-name: 'Production'
          package: ./publish